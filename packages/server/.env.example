DATABASE_URL="postgresql://postgres:postgres@localhost:5432/investpro?schema=public"
JWT_SECRET="investpro-dev-jwt-secret-2024"
JWT_REFRESH_SECRET="investpro-dev-refresh-secret-2024"
PORT=3001
NODE_ENV=development
# Em produção: URL do frontend no Vercel (obrigatório quando NODE_ENV=production)
CORS_ORIGIN="http://localhost:5173"
# URL do frontend usada para montar o link de reset de senha (nunca derivada do header Host/Origin da request)
FRONTEND_URL="http://localhost:5173"
# Chave da API do Resend (resend.com), usada para enviar emails transacionais (reset de senha, etc.)
RESEND_API_KEY=""
# Remetente dos emails enviados via Resend. Requer domínio verificado no Resend em produção.
RESEND_FROM_EMAIL="InvestPro <onboarding@resend.dev>"