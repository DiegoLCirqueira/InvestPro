{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "watchPatterns": [
      "packages/server/**",
      "packages/shared/**",
      "package-lock.json"
    ],
    "buildCommand": "npm install && cd packages/server && npx prisma generate && npm run build"
  },
  "deploy": {
    "startCommand": "cd packages/server && npx prisma migrate deploy && npx tsx dist/index.js",
    "healthcheckPath": "/health",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}