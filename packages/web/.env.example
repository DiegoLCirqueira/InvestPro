# InvestPro — frontend (packages/web)
# Copie para .env nesta mesma pasta. O Vite lê .env automaticamente na raiz do pacote.

# URL base da API consumida pelo frontend em tempo de build.
VITE_API_URL="http://localhost:3001/api/v1"
